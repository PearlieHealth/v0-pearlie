# TikTok Pixel Implementation — Copy-Paste Prompt

Copy everything below the line and paste it as the prompt for a new Claude Code session.

---

Implement TikTok Pixel with Manual Advanced Matching. The env var `NEXT_PUBLIC_TIKTOK_PIXEL_ID` will be set in Vercel — never hardcode the ID. Read each file before editing. Follow these 7 tasks exactly.

TASK 1 — Create `lib/tiktok-pixel.ts` (new file). This helper module must export three things: (A) `identifyForTikTok({ email, phone, externalId })` — an async function that hashes each value with SHA-256 via `crypto.subtle.digest` (trim + lowercase before hashing, hex-encode output), normalises UK phone numbers (strip non-digits, convert leading `0` to `+44`, then hash), checks `hasConsentForMarketing()` from `@/lib/cookie-consent` and `window.ttq` existence before calling `window.ttq.identify({ email, phone_number, external_id })` with the hashed values. (B) `trackTikTokEvent(event: string, params?: Record<string, unknown>)` — checks consent + `window.ttq` existence, then calls `window.ttq.track(event, params)`. (C) A `declare global` block extending `Window` with `ttq` typed with methods: `load`, `page`, `track`, `identify`. Both functions must return early if `typeof window === "undefined"` (SSR safety).

TASK 2 — Edit `components/analytics-scripts.tsx`. After the Meta Pixel block (ends at line 62), add the TikTok pixel script block wrapped in `{marketingConsent && process.env.NEXT_PUBLIC_TIKTOK_PIXEL_ID && (...)}` using `<Script id="tiktok-pixel" strategy="afterInteractive">`. Use the standard TikTok base code from their docs that calls `ttq.load('${process.env.NEXT_PUBLIC_TIKTOK_PIXEL_ID}')` then `ttq.page()`. This handles automatic PageView tracking on every page.

TASK 3 — Edit `app/intake/page.tsx`. Add import for `identifyForTikTok` and `trackTikTokEvent` from `@/lib/tiktok-pixel`. At line 464, immediately BEFORE the existing `trackEvent("lead_submitted", ...)` call, add: `await identifyForTikTok({ email: formData.email, phone: formData.phone, externalId: leadId })` then `trackTikTokEvent("CompleteRegistration", { content_name: "intake_form", treatment: formData.treatments.join(", "), postcode: formData.postcode, flow: isEmergency ? "emergency" : "planning", urgency: isEmergency ? formData.urgency : null, cost_approach: formData.costApproach || null })`. The `handleSubmit` is already async (line 384) so `await` works.

TASK 4 — Edit `components/clinic/direct-enquiry-form.tsx`. Add import for `identifyForTikTok` and `trackTikTokEvent` from `@/lib/tiktok-pixel`. (A) At line 75, immediately BEFORE the existing `trackEvent("lead_submitted", ...)` call, add: `await identifyForTikTok({ email: email.trim(), phone: phone.trim(), externalId: data.leadId })` then `trackTikTokEvent("CompleteRegistration", { content_name: "direct_enquiry", treatment: treatment || null })`. (B) At line 94, inside the `onVerified` callback of the OTP verification step, add BEFORE the existing `trackEvent("email_verified", ...)`: `await identifyForTikTok({ email: email.trim(), phone: phone.trim(), externalId: leadId })` then `trackTikTokEvent("CompleteRegistration", { content_name: "otp_verified_direct" })`. The `onVerified` callback is currently synchronous — you need to make it async: change `onVerified={() => {` to `onVerified={async () => {`.

TASK 5 — Edit `app/match/[matchId]/page.tsx`. Add import for `identifyForTikTok` and `trackTikTokEvent` from `@/lib/tiktok-pixel`. Add TikTok events at these 3 locations: (A) At line 438, the `handleVerificationSuccess` function — this is where OTP is verified. The page has `leadEmail` (line 111) available. Add BEFORE the existing `trackEvent("email_verified", ...)` at line 440: `await identifyForTikTok({ email: leadEmail || undefined, externalId: leadId || undefined })` then `trackTikTokEvent("CompleteRegistration", { content_name: "otp_verified_match" })`. The function is currently synchronous — change `const handleVerificationSuccess = (data?) => {` to `const handleVerificationSuccess = async (data?) => {`. (B) At line 935, the "Message Clinic" link — this is a `<Link>` not a button with an onClick handler, so add an `onClick` to the `<Button>` wrapping the Link that calls `trackTikTokEvent("Contact", { content_name: "message_clinic_match_page" })` before the navigation. (C) At line 921, the `onSelectSlot` callback which navigates to `/booking/confirm` — add `trackTikTokEvent("InitiateCheckout", { content_name: "select_time_slot" })` before the `window.location.href` redirect.

TASK 6 — Edit `components/clinic/profile/clinic-profile-content.tsx`. Add import for `trackTikTokEvent` from `@/lib/tiktok-pixel`. Add TikTok events at these 4 locations: (A) At line 190 next to the existing `trackEvent("book_clicked", ...)` inside `handleBookAppointment`, add: `trackTikTokEvent("PlaceAnOrder", { content_name: "confirm_request" })`. (B) At line 269 next to the existing `trackEvent("booking_confirmed_inline", ...)` inside `handleConfirmBooking`, add: `trackTikTokEvent("PlaceAnOrder", { content_name: "booking_confirmed_inline" })`. (C) At line 607, the "Message Clinic" button `onClick={() => setShowChat(!showChat)}` — extend the onClick to also call `trackTikTokEvent("Contact", { content_name: "message_clinic_profile" })`. (D) At line 822, the mobile "Message Clinic" button `onClick={() => setShowMobileChat(true)}` — extend the onClick to also call `trackTikTokEvent("Contact", { content_name: "message_clinic_profile_mobile" })`.

TASK 7 — Edit `app/booking/confirm/page.tsx`. Add import for `trackTikTokEvent` from `@/lib/tiktok-pixel`. At line 117 where `setConfirmed(true)` is called after successful booking request, add: `trackTikTokEvent("PlaceAnOrder", { content_name: "booking_confirmed_standalone" })`.

Event mapping summary for reference: `lead_submitted` (intake form) → TikTok `CompleteRegistration` + `identify`, `lead_submitted` (direct enquiry) → TikTok `CompleteRegistration` + `identify`, `email_verified` (OTP on match page) → TikTok `CompleteRegistration` + `identify`, `email_verified` (OTP on direct enquiry) → TikTok `CompleteRegistration` + `identify`, `book_clicked` / confirm request → TikTok `PlaceAnOrder`, `booking_confirmed_inline` → TikTok `PlaceAnOrder`, booking confirm standalone page → TikTok `PlaceAnOrder`, select time slot on match page → TikTok `InitiateCheckout`, message clinic click → TikTok `Contact`, every page load → TikTok `PageView` (automatic via `ttq.page()`). Note: `CompletePayment` is NOT implemented here — it would fire when a clinic confirms an appointment on their dashboard, but that's a clinic-side action (different browser/user) requiring the TikTok server-side Events API, which is a separate project.

Do NOT enable Automatic Advanced Matching in TikTok Events Manager. Do NOT send unhashed PII — always use the SHA-256 helper. Every TikTok call must be gated on marketing consent. Run `npm run build` and `npm run lint` when done. Commit and push.
